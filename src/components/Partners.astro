---
const partners = [
  { name: "McLaren", logo: "/images/partners/mclaren.svg" },
  { name: "Pirelli", logo: "/images/partners/pirelli.svg" },
  { name: "Honda", logo: "/images/partners/honda.svg" },
];
---

<section class="partners" id="partners">
  <div class="container">
    <div class="partners__header">
      <span class="section-label reveal">Trusted By</span>
      <h2 class="partners__title reveal reveal-delay-1">Our Partners</h2>
      <p class="partners__intro reveal reveal-delay-2">
        We work with leading teams, manufacturers, and sponsors in the motorsport industry.
      </p>
    </div>

    <div class="partners__grid reveal">
      {partners.map((partner, index) => (
        <div class="partner" style={`--delay: ${index * 0.1}s`}>
          <div class="partner__logo">
            <span class="partner__name">{partner.name}</span>
          </div>
        </div>
      ))}
    </div>

    <div class="partners__cta reveal">
      <p class="partners__cta-text">Interested in partnering with JBMM?</p>
      <a href="#contact" class="btn btn--outline">
        Become a Partner
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .partners {
    padding: var(--space-xl) 0;
    background: var(--color-bg-elevated);
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .partners__header {
    text-align: center;
    max-width: 600px;
    margin: 0 auto var(--space-xl);
  }

  .section-label {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: var(--space-sm);
  }

  .partners__title {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: var(--space-sm);
  }

  .partners__intro {
    color: var(--color-text-muted);
  }

  .partners__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-sm);
    max-width: 900px;
    margin: 0 auto var(--space-lg);
  }

  @media (min-width: 640px) {
    .partners__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .partner {
    aspect-ratio: 3/1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-md);
    background: var(--color-bg);
    border: 1px solid rgba(255, 255, 255, 0.05);
    transition: var(--transition-base);
  }

  .partner:hover {
    border-color: rgba(255, 255, 255, 0.1);
    background: var(--color-surface);
  }

  .partner__logo {
    opacity: 0.5;
    transition: var(--transition-fast);
  }

  .partner:hover .partner__logo {
    opacity: 1;
  }

  .partner__name {
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2vw, 1.5rem);
    letter-spacing: 0.1em;
    color: var(--color-text-muted);
  }

  .partner:hover .partner__name {
    color: var(--color-text);
  }

  .partners__cta {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
  }

  @media (min-width: 640px) {
    .partners__cta {
      flex-direction: row;
      justify-content: center;
    }
  }

  .partners__cta-text {
    color: var(--color-text-muted);
    font-size: 0.9375rem;
  }
</style>
